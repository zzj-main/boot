继承中构造方法的访问特点
子类中所有的构造方法默认都会访问父类中无参的构造方法
o:因为子类会继承父类中的数据，可能还会使用父类的数据，所以，子类初始化之前，一定要先完成父类数据的初始化。
o:每一个子类构造方法的第一条语句默认都是：super(); 所以会调用 父类的无参构造方法
通过子类对象访问一个方法
o:首先在子类成员范围内寻找
o:然后在父类成员范围内寻找
o:找不到会报错
注意事项：
java中类只支持单继承，不支持多继承
java中类支持多层集成 a extends b ,b extends c

方法重写
子类中出现和父类一模一样的方法声明
当子类需要父类的功能，而功能主体子类有自己特有的内容，可以重写父类中的方法，即沿袭了父类的功能，又定义了子类特有的内容
@override
方法重写的的注解，可以帮助我们检查重写方法的声明正确性。
注意事项：
父类的私有方法，子类不能重写
子类方法访问权限不能更低 （public > 默认 >private）

final修饰的基本类型的数据值不能改变
final修饰的引用类型的地址值不能改变，地址里的内容是可以改变的
